{
  "name": "rplaner",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/rplaner/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/rplaner",
        "main": "apps/rplaner/src/main.ts",
        "tsConfig": "apps/rplaner/tsconfig.app.json",
        "assets": ["apps/rplaner/src/assets"],
        "isolatedConfig": true,
        "webpackConfig": "apps/rplaner/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "rplaner:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "rplaner:build:development"
        },
        "production": {
          "buildTarget": "rplaner:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/rplaner/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/rplaner/jest.config.ts"
      }
    }
  },
  "tags": []
}
